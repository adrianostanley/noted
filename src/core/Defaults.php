<?php

declare(strict_types=1);

namespace Noted\Core;

/**
 * This class basically stores constants.
 *
 * @author Adriano
 */
class Defaults {
    
    /**
     * By default, all IDs will be zero, considering they won't be stored.
     */
    const ID = 0;
    
    /**
     * By default, all texts will be an empty text.
     */
    const TEXT = '';
    
    
}
